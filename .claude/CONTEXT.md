# Context — diamonds_bc

> Load tier: warm
> Project-specific only. See CLAUDE.md for stack and constraints.

## Purpose
Bootcamp project: refactor a Jupyter notebook into a production-ready Python ML package.
Predict diamond prices from raw features using scikit-learn.

## Contributors
- Camille (`camille/`)
- Boubachar (`boubachar/`)

## Git workflow
- Base branch: `0-Starter-productionizing-ml` (upstream: `vivadata/diamonds`)
- Fork: `CamilleLarpin/diamonds_b_and_c` — push all branches here
- One feature branch per module (e.g. `camille/data`, `camille/model`)
- PR required before merge — no self-merge

## Current state (2026-03-09)
- `data.py` — implemented, PR #1 open for review
- `model.py`, `registry.py`, `train.py` — stubs only
